#include <iostream>
using namespace std;
int main()
{
    char ch;
    int number = 32, up, lo, value;
    cout << "enter the vowel:";
    cin >> ch;
    if (number + lo)
    {
        value = lo + number;
        cout << "value";
    }
    else if (up + number)
    {
        value = up + number;
        cout << "value";
    }
    return 0;
}
